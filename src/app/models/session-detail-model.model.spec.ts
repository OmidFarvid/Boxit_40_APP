import { SessionDetailModel } from './session-detail-modelmodel';

describe('SessionDetailModel', () => {
  it('should create an instance', () => {
    expect(new SessionDetailModel()).toBeTruthy();
  });
});
