import { Session } from "./session.model";

export class SessionDetail {
  Session: Session;
  ParcelCount: number;
  DetectedParcelCount: number;
  SortedParcelCount: number;
  RegionCount: number;
  GatesCount: number;
}
